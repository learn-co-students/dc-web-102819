<h1> <%= @user.name %> </h1>

<h2> Locations Visited by <%= @user.name%> </h2>

<ul>
  <% @user.locations.each do |location| %>
  <li><%= location.name %> </li>
<%end %>

</ul>

<%= link_to "Edit User", edit_user_path(@user)%>
<%= button_to "Delete User", user_path(@user), method: :delete %>